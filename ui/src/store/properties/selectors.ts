export const getProperties = state => state.properties.data;

export const getTotalProperties = state => state.properties.total;

export const getCurrentPropertiesPage = state => state.properties.page;
